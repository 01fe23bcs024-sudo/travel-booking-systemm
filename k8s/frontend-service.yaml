apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: travel-booking
  labels:
    app: frontend
spec:
  type: LoadBalancer
  selector:
    app: frontend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    name: http
